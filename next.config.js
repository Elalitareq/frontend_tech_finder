/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            "i.ibb.co",
            "image.shutterstock.com"
        ]
    }
}

module.exports = nextConfig
